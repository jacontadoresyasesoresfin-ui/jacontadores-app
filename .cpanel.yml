---
deployment:
  tasks:
    # El despliegue se realiza automaticamente via GitHub Actions (.github/workflows/deploy.yml)
    # Este archivo se mantiene como placeholder para que cPanel no de error.
    # GitHub Actions construye con Node.js 20 y sube el build compilado via SSH.
    - echo "Deployment is handled by GitHub Actions CI/CD pipeline"
